// Task 4: Client-side Validation
console.log("Event Management System Loaded");

// Example: Simple alert when clicking a register button
document.querySelectorAll('.btn-sm').forEach(button => {
    button.addEventListener('click', () => {
        console.log("User clicked registration");
    });
});